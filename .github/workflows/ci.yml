name: Diagnose Online maintenance hotfix

on:
  push:
    branches:
      - fix/online-single-attack-combat

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          cache: npm
      - run: npm ci
      - name: Run focused Online maintenance regression
        id: validate
        shell: bash
        run: |
          set +e
          node --test tests/online-maintenance-choice.test.mjs > /tmp/online-validation.log 2>&1
          code=$?
          echo "code=$code" >> "$GITHUB_OUTPUT"
          exit 0
      - name: Publish focused diagnostic to PR 26
        env:
          GH_TOKEN: ${{ github.token }}
          EXIT_CODE: ${{ steps.validate.outputs.code }}
          GH_REPO: ${{ github.repository }}
        shell: bash
        run: |
          status="FAILED"
          if [ "$EXIT_CODE" = "0" ]; then status="PASSED"; fi
          {
            echo "Focused Online maintenance regression: **$status** (exit $EXIT_CODE)"
            echo
            echo '```text'
            cat /tmp/online-validation.log
            echo '```'
          } > /tmp/pr-comment.md
          gh api "repos/$GH_REPO/issues/26/comments" --method POST -F body=@/tmp/pr-comment.md
      - name: Fail job when focused regression failed
        if: steps.validate.outputs.code != '0'
        run: exit 1
