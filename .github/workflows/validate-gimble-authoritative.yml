name: Validate Gimble authoritative migration

# Rerun after aligning hero-priority fixtures with target legality.
on:
  push:
    branches:
      - fix/cards_mechanics
    paths:
      - .github/workflows/validate-gimble-authoritative.yml

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/cards_mechanics
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: chmod +x scripts/*.sh
      - run: node --test tests/gimble-authoritative-hero.test.mjs tests/hero-priority-response.test.mjs tests/hero-priority-ui.test.mjs
      - run: npm run build
      - name: Remove one-shot validator
        run: |
          rm .github/workflows/validate-gimble-authoritative.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .github/workflows/validate-gimble-authoritative.yml
          git commit -m "chore: remove Gimble migration validator"
          git push origin HEAD:fix/cards_mechanics
