name: Disabled temporary Quarion repair runner

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary runner disabled; safe to delete."
