name: Apply support and turn counter fix

on:
  push:
    branches:
      - fix/cards_mechanics
    paths:
      - scripts/apply-support-turn-counter-fix.mjs
      - scripts/finalize-support-turn-counter-fix.mjs
      - scripts/fix-support-double-count.mjs
      - package.json
      - .github/workflows/apply-support-turn-counter-fix.yml

permissions:
  contents: write

jobs:
  apply-and-verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          cache: npm
      - run: npm ci
      - name: Apply canonical rules migration
        run: npm run rules:migrate
      - name: Prevent duplicate Support stat application
        run: node scripts/fix-support-double-count.mjs
      - name: Refresh card implementation audit
        run: node scripts/export-card-implementation-audit.mjs
      - name: Persist generated rule changes
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app/rules-engine/compiler.mjs app/rules-engine/engine.mjs app/rules-engine/effects.mjs app/rules-engine/card-rules.mjs docs/card-implementation-audit.json docs/card-implementation-audit.md
          if git diff --cached --quiet; then
            echo 'Rule files already canonical.'
            exit 0
          fi
          git commit -m 'fix(rules): enforce support adjacency and canonical card counters'
          git push origin HEAD:fix/cards_mechanics