--- a/app/page.tsx
+++ b/app/page.tsx
@@ -15,7 +15,7 @@
 import { cardPlayTargetPolicy, isValidTarget, targetPolicy, TargetScope } from "./rules-engine/targeting.mjs";
 import { applyCloneRetaliation, claimOncePerTurn, earthquakeDamage, elementalChainFrom as ruleElementalChainFrom } from "./game-rules.mjs";
 import { clearOnlineSession, loadOnlineSession, saveOnlineSession } from "./online-session.mjs";
-import { MAIN_DECK_SIZE, MAX_COPIES, USER_DECK_STORAGE_KEY, cardAllowedInDeckZone, defaultUserDeck, disabledDeckCardIds as sharedDisabledDeckCardIds, expandUserDeckMain, removedCatalogPages as sharedRemovedCatalogPages, resolveUserDeckExtra, suppliedDeckPages as sharedSuppliedDeckPages, validateUserDeck } from "./user-deck.mjs";
+import { MAIN_DECK_SIZE, MAX_COPIES, USER_DECK_STORAGE_KEY, cardAllowedInDeckZone, defaultUserDeck, disabledDeckCardIds as sharedDisabledDeckCardIds, expandUserDeckMain, removedCatalogPages as sharedRemovedCatalogPages, resolveUserDeckExtra, suppliedDeckPages as sharedSuppliedDeckPages, validateUserDeck, validateUserDeckDraft } from "./user-deck.mjs";
 import type { UserDeck } from "./user-deck.mjs";
 import { immediateDirectCombatPresentation, resolvedCombatPresentation } from "./combat-presentation.mjs";
 import { CombatAnimation } from "./match/combat-animation";
@@ -304,19 +304,27 @@
  </section>
 }
 
+function DeckQuantityControls({cardName,quantity,canIncrease,onDecrease,onIncrease}:{cardName:string;quantity:number;canIncrease:boolean;onDecrease:()=>void;onIncrease:()=>void}){
+ return <div className="deck-quantity-controls" aria-label={`${cardName}: ${quantity} de ${MAX_COPIES} cópias no Deck Principal`}>
+  <button type="button" aria-label={`Remover uma cópia de ${cardName}`} title="Remover uma cópia" disabled={quantity<=0} onClick={onDecrease}>−</button>
+  <output aria-live="polite"><b>{quantity}</b><small>/{MAX_COPIES}</small></output>
+  <button type="button" aria-label={`Adicionar uma cópia de ${cardName}`} title="Adicionar uma cópia" disabled={!canIncrease} onClick={onIncrease}>+</button>
+ </div>
+}
+
 const createDefaultUserDecks=()=>Object.fromEntries(deckDefs.map(deck=>[deck.id,defaultUserDeck(deck.id,cards,deck.name)])) as Record<DeckId,UserDeck>;
 const deckValidationLabel=(error:string)=>error.includes("exactly 49")?"o Deck Principal precisa ter exatamente 49 cartas":error.includes("copy")||error.includes("quantity")?"cada carta pode ter no máximo 3 cópias":error.includes("identity")?"há carta fora da identidade deste herói":error.includes("unavailable")?"há carta indisponível na lista":error.includes("extra deck")?"o Deck Extra contém uma carta inválida":error.includes("duplicate")?"há entradas duplicadas":error;
 
 export default function Home(){
  const [screen,setScreen]=useState<Screen>("menu");const[mode,setMode]=useState<"bot"|"online">("bot");const[mine,setMine]=useState<DeckId>("gimble");const[enemy,setEnemy]=useState<DeckId>("goblin");const[difficulty,setDifficulty]=useState("Normal");const[game,setGame]=useState<Game|null>(null);const[maintenanceOpen,setMaintenanceOpen]=useState(false);const[showLog,setShowLog]=useState(false);const[showInspector,setShowInspector]=useState<CardDef|null>(null);const[targeting,setTargeting]=useState<Targeting|null>(null);const[imageChoice,setImageChoice]=useState<ImageChoice|null>(null);const[cafeChoice,setCafeChoice]=useState<number|null>(null);const[responseWindow,setResponseWindow]=useState<PendingResponse|null>(null);const[combatAction,setCombatAction]=useState<CombatAction|null>(null);const[onlineCombatPresentation,setOnlineCombatPresentation]=useState<CombatAction|null>(null);const[aiAttackQueue,setAiAttackQueue]=useState<string[]>([]);const[visualFx,setVisualFx]=useState<VisualFx|null>(null);const[confirmSurrender,setConfirmSurrender]=useState(false);const[extraView,setExtraView]=useState<{kind?:"extra"|"grave";title:string;cards:CardDef[]}|null>(null);const[searchChoice,setSearchChoice]=useState<SearchRequest|null>(null);const[shufflingDeck,setShufflingDeck]=useState<0|1|null>(null);const[dragging,setDragging]=useState<{index:number;type:CardType}|null>(null);
-const [collectionQuery,setCollectionQuery]=useState("");const [collectionType,setCollectionType]=useState<"Todas"|CardType>("Todas");const deferredCollectionQuery=useDeferredValue(collectionQuery);
+const [collectionQuery,setCollectionQuery]=useState("");const [collectionType,setCollectionType]=useState<"Todas"|CardType>("Todas");const [collectionMembership,setCollectionMembership]=useState<"Todas"|"No deck"|"Fora do deck">("Todas");const [collectionSort,setCollectionSort]=useState<"Custo"|"Nome"|"Tipo">("Custo");const deferredCollectionQuery=useDeferredValue(collectionQuery);
 const [userDecks,setUserDecks]=useState<Record<DeckId,UserDeck>>(()=>createDefaultUserDecks());
 const [deckUndo,setDeckUndo]=useState<Partial<Record<DeckId,UserDeck[]>>>({});
 const [deckStorageReady,setDeckStorageReady]=useState(false);
 const [deckDrag,setDeckDrag]=useState<{cardId:string;zone:"collection"|"main"|"extra"}|null>(null);
 const persistedDecksRef=useRef<Record<string,UserDeck>>({});
-useEffect(()=>{const defaults=createDefaultUserDecks(),next={...defaults};try{const raw=localStorage.getItem(USER_DECK_STORAGE_KEY),parsed=raw?JSON.parse(raw):null,candidates=parsed?.decks??parsed;if(candidates&&typeof candidates==="object")deckDefs.forEach(deck=>{const validation=validateUserDeck(candidates[deck.id],cards);if(validation.ok&&validation.deck)next[deck.id]=validation.deck})}catch{}persistedDecksRef.current=Object.fromEntries(deckDefs.map(deck=>[deck.id,next[deck.id]]));setUserDecks(next);setDeckStorageReady(true)},[]);
-useEffect(()=>{if(!deckStorageReady)return;const persisted={...persistedDecksRef.current};deckDefs.forEach(deck=>{const validation=validateUserDeck(userDecks[deck.id],cards);if(validation.ok&&validation.deck)persisted[deck.id]=validation.deck});persistedDecksRef.current=persisted;localStorage.setItem(USER_DECK_STORAGE_KEY,JSON.stringify({version:1,decks:persisted}))},[userDecks,deckStorageReady]);
+useEffect(()=>{const defaults=createDefaultUserDecks(),next={...defaults};try{const raw=localStorage.getItem(USER_DECK_STORAGE_KEY),parsed=raw?JSON.parse(raw):null,candidates=parsed?.decks??parsed;if(candidates&&typeof candidates==="object")deckDefs.forEach(deck=>{const validation=validateUserDeckDraft(candidates[deck.id],cards);if(validation.ok&&validation.deck)next[deck.id]=validation.deck})}catch{}persistedDecksRef.current=Object.fromEntries(deckDefs.map(deck=>[deck.id,next[deck.id]]));setUserDecks(next);setDeckStorageReady(true)},[]);
+useEffect(()=>{if(!deckStorageReady)return;const persisted={...persistedDecksRef.current};deckDefs.forEach(deck=>{const validation=validateUserDeckDraft(userDecks[deck.id],cards);if(validation.ok&&validation.deck)persisted[deck.id]=validation.deck});persistedDecksRef.current=persisted;localStorage.setItem(USER_DECK_STORAGE_KEY,JSON.stringify({version:1,decks:persisted}))},[userDecks,deckStorageReady]);
 const [engineTargetSelection,setEngineTargetSelection]=useState<string[]>([]);
 const [engineChoiceIndex,setEngineChoiceIndex]=useState<number|null>(null);
 const [repositionSeconds,setRepositionSeconds]=useState(30);const aiRepositionHandledRef=useRef<string>("");
@@ -914,13 +922,16 @@
  useEffect(()=>{const pending=game?.pendingResponse;if(mode!=="bot"||pending?.responder!==0||!pending.deadline||pending.deadline>clockNow)return;const key=`${pending.actor}:${pending.responder}:${pending.passes??0}:${pending.action}:${pending.deadline}`,current=currentGameRef.current?.pendingResponse,currentKey=current?`${current.actor}:${current.responder}:${current.passes??0}:${current.action}:${current.deadline??0}`:"";if(currentKey===key)void passPriorityWindow(0,true)},[clockNow,mode,game?.pendingResponse?.actor,game?.pendingResponse?.responder,game?.pendingResponse?.passes,game?.pendingResponse?.action,game?.pendingResponse?.deadline]);
 
  const selectedDeck=deckById(mine),activeUserDeck=userDecks[mine]??defaultUserDeck(mine,cards,selectedDeck.name),deckValidation=validateUserDeck(activeUserDeck,cards);
- const selectedPool=useMemo(()=>activeUserDeck.main.map(entry=>{const card=cards.find(candidate=>candidate.id===entry.cardId);return card?{...card,collectionQuantity:entry.quantity}:null}).filter((card):card is CardDef=>!!card),[activeUserDeck]);
+ const selectedPool=useMemo<CardDef[]>(()=>activeUserDeck.main.flatMap(entry=>{const card=cards.find(candidate=>candidate.id===entry.cardId);return card?[{...card,collectionQuantity:entry.quantity}]:[]}),[activeUserDeck]);
  const selectedExtra=useMemo(()=>activeUserDeck.extra.map(cardId=>cards.find(card=>card.id===cardId)).filter((card):card is CardDef=>!!card),[activeUserDeck]);
  const availableDeckPool=useMemo(()=>cards.filter(card=>cardAllowedInDeckZone(mine,card,card.imageCard?"extra":"main")),[mine]);
- const collectionMatches=(card:CardDef)=>{const query=cleanName(deferredCollectionQuery.trim());return (collectionType==="Todas"||card.type===collectionType)&&(!query||cleanName(`${card.name} ${card.type} ${card.text} ${(card.tags||[]).join(" ")} ${(card.subtypes||[]).join(" ")}`).includes(query))};
- const filteredSelectedPool=selectedPool.filter(collectionMatches),filteredSelectedExtra=selectedExtra.filter(collectionMatches),filteredAvailableDeckPool=availableDeckPool.filter(collectionMatches);
  const mainDeckCopies=deckValidation.mainCount,deckListValid=deckValidation.ok,activeUndo=deckUndo[mine]||[];
  const selectedMainQuantity=(cardId:string)=>activeUserDeck.main.find(entry=>entry.cardId===cardId)?.quantity||0;
+ const collectionMatches=(card:CardDef)=>{const query=cleanName(deferredCollectionQuery.trim());return (collectionType==="Todas"||card.type===collectionType)&&(!query||cleanName(`${card.name} ${card.type} ${card.text} ${(card.tags||[]).join(" ")} ${(card.subtypes||[]).join(" ")}`).includes(query))};
+ const cardIsInActiveDeck=(card:CardDef)=>card.imageCard?activeUserDeck.extra.includes(card.id):selectedMainQuantity(card.id)>0;
+ const membershipMatches=(card:CardDef)=>collectionMembership==="Todas"||collectionMembership==="No deck"&&cardIsInActiveDeck(card)||collectionMembership==="Fora do deck"&&!cardIsInActiveDeck(card);
+ const compareCollectionCards=(left:CardDef,right:CardDef)=>collectionSort==="Nome"?left.name.localeCompare(right.name,"pt-BR"):collectionSort==="Tipo"?left.type.localeCompare(right.type,"pt-BR")||left.cost-right.cost||left.name.localeCompare(right.name,"pt-BR"):left.cost-right.cost||left.name.localeCompare(right.name,"pt-BR");
+ const filteredSelectedPool=selectedPool.filter(collectionMatches).sort(compareCollectionCards),filteredSelectedExtra=selectedExtra.filter(collectionMatches).sort(compareCollectionCards),filteredAvailableDeckPool=availableDeckPool.filter(card=>collectionMatches(card)&&membershipMatches(card)).sort(compareCollectionCards);
  const mutateActiveDeck=(mutator:(deck:UserDeck)=>void)=>{const previous=structuredClone(activeUserDeck),next=structuredClone(activeUserDeck);mutator(next);setDeckUndo(history=>({...history,[mine]:[...(history[mine]||[]).slice(-19),previous]}));setUserDecks(current=>({...current,[mine]:next}))};
  const setMainQuantity=(cardId:string,quantity:number)=>{const card=cards.find(candidate=>candidate.id===cardId);if(!card||!cardAllowedInDeckZone(mine,card,"main"))return;const current=selectedMainQuantity(cardId),next=Math.max(0,Math.min(MAX_COPIES,Math.trunc(quantity)));if(next>current&&mainDeckCopies+(next-current)>MAIN_DECK_SIZE)return;mutateActiveDeck(deck=>{const index=deck.main.findIndex(entry=>entry.cardId===cardId);if(next===0){if(index>=0)deck.main.splice(index,1);return}if(index>=0)deck.main[index]={cardId,quantity:next};else deck.main.push({cardId,quantity:next})})};
  const toggleExtraCard=(cardId:string,force?:boolean)=>{const card=cards.find(candidate=>candidate.id===cardId);if(!card||!cardAllowedInDeckZone(mine,card,"extra"))return;mutateActiveDeck(deck=>{const has=deck.extra.includes(cardId),next=force??!has;if(next&&!has)deck.extra.push(cardId);else if(!next&&has)deck.extra=deck.extra.filter(id=>id!==cardId)})};
@@ -928,6 +939,7 @@
  const handleDeckDrop=(target:"collection"|"main"|"extra")=>{const drag=deckDrag;if(!drag)return;setDeckDrag(null);if(drag.zone===target)return;if(target==="collection"){if(drag.zone==="main")setMainQuantity(drag.cardId,selectedMainQuantity(drag.cardId)-1);else if(drag.zone==="extra")toggleExtraCard(drag.cardId,false);return}if(target==="main")addDeckCard(drag.cardId,"main");else addDeckCard(drag.cardId,"extra")};
  const undoActiveDeck=()=>{const stack=deckUndo[mine]||[],previous=stack.at(-1);if(!previous)return;setUserDecks(current=>({...current,[mine]:structuredClone(previous)}));setDeckUndo(history=>({...history,[mine]:(history[mine]||[]).slice(0,-1)}))};
  const resetActiveDeck=()=>{const fresh=defaultUserDeck(mine,cards,selectedDeck.name),previous=structuredClone(activeUserDeck);setDeckUndo(history=>({...history,[mine]:[...(history[mine]||[]).slice(-19),previous]}));setUserDecks(current=>({...current,[mine]:fresh}))};
+ const clearActiveDeck=()=>mutateActiveDeck(deck=>{deck.main=[];deck.extra=[]});
  const renameActiveDeck=(name:string)=>setUserDecks(current=>({...current,[mine]:{...current[mine],name:name.slice(0,60)}}));
  const myRoomParticipant=isHost?roomInfo?.host:roomInfo?.guest;
  const opponentRoomParticipant=isHost?roomInfo?.guest:roomInfo?.host;
@@ -1066,13 +1078,13 @@
    <div className="deck-detail"><aside style={{"--deck":selectedDeck.color} as React.CSSProperties}>
     <button className="collection-hero-inspect" onClick={()=>setShowInspector(cards.find(card=>card.page===selectedDeck.heroPage)||null)} aria-label={`Ver detalhes de ${selectedDeck.name}`}><RemoteCardArt page={selectedDeck.heroPage} name={selectedDeck.name} priority/></button>
     <h3>{selectedDeck.name}</h3><p>{selectedDeck.faction} · {selectedDeck.style}</p><label className="deck-name-field"><span>Nome do deck</span><input value={activeUserDeck.name} maxLength={60} onChange={event=>renameActiveDeck(event.target.value)} aria-label="Nome do deck"/></label><b>{mainDeckCopies}/{MAIN_DECK_SIZE} cartas no Deck Principal</b><strong className="extra-summary">Deck Extra · {selectedExtra.length} Imagens</strong>
-    <span className={`deck-validity ${deckListValid?"is-valid":"is-invalid"}`}>{deckListValid?"✓ Lista válida e salva automaticamente":`⚠ ${deckValidation.errors.slice(0,3).map(deckValidationLabel).join(" · ")}`}</span><div className="deck-edit-actions"><button disabled={!activeUndo.length} onClick={undoActiveDeck}>↶ Desfazer</button><button onClick={resetActiveDeck}>Restaurar padrão</button></div>
+    <span className={`deck-validity ${deckListValid?"is-valid":"is-invalid"}`}>{deckListValid?"✓ Lista válida e salva automaticamente":`⚠ Rascunho salvo · ${deckValidation.errors.slice(0,3).map(deckValidationLabel).join(" · ")}`}</span><div className="deck-capacity" role="progressbar" aria-label={`${mainDeckCopies} de ${MAIN_DECK_SIZE} cartas no Deck Principal`} aria-valuemin={0} aria-valuemax={MAIN_DECK_SIZE} aria-valuenow={mainDeckCopies}><span style={{"--deck-progress":`${Math.min(100,mainDeckCopies/MAIN_DECK_SIZE*100)}%`} as React.CSSProperties}/><b>{mainDeckCopies}/{MAIN_DECK_SIZE}</b></div><div className="deck-edit-actions"><button disabled={!activeUndo.length} onClick={undoActiveDeck}>↶ Desfazer</button><button disabled={!activeUserDeck.main.length&&!activeUserDeck.extra.length} onClick={clearActiveDeck}>Limpar lista</button><button onClick={resetActiveDeck}>Restaurar padrão</button></div>
     <button className="gold" disabled={!deckListValid} onClick={()=>setScreen("setup")}>Usar este deck</button>
    </aside><HeroGuide deck={selectedDeck}/><div className="collection-lists">
-    <div className="collection-toolbar" role="search"><label><span>Buscar cartas</span><input type="search" value={collectionQuery} onChange={event=>setCollectionQuery(event.target.value)} placeholder="Nome, efeito, palavra-chave…"/></label><label><span>Tipo</span><select value={collectionType} onChange={event=>setCollectionType(event.target.value as "Todas"|CardType)}><option>Todas</option>{(["Criatura","Feitiço","Artefato","Encanto","Terreno"] as CardType[]).map(type=><option key={type}>{type}</option>)}</select></label><output aria-live="polite">{filteredSelectedPool.length+filteredSelectedExtra.length} resultado(s)</output></div>
-    <section className={`deck-build-zone deck-main-zone ${deckDrag?"is-dragging":""}`} onDragOver={event=>event.preventDefault()} onDrop={event=>{event.preventDefault();handleDeckDrop("main")}}><header><b>Deck Principal</b><span>{mainDeckCopies}/{MAIN_DECK_SIZE} cartas · arraste da coleção ou use + / −.</span></header>{filteredSelectedPool.length?<div className="card-library deck-selected-library">{filteredSelectedPool.map(c=><div className="deck-card-entry" key={c.id} draggable onDragStart={()=>setDeckDrag({cardId:c.id,zone:"main"})} onDragEnd={()=>setDeckDrag(null)}><OriginalCard card={c} small onClick={()=>setShowInspector(c)}/><div className="deck-quantity-controls" aria-label={`${c.name}: ${c.collectionQuantity||1} cópias`}><button aria-label={`Remover uma cópia de ${c.name}`} onClick={()=>setMainQuantity(c.id,(c.collectionQuantity||1)-1)}>−</button><b>{c.collectionQuantity||1}</b><button aria-label={`Adicionar uma cópia de ${c.name}`} disabled={(c.collectionQuantity||1)>=MAX_COPIES||mainDeckCopies>=MAIN_DECK_SIZE} onClick={()=>setMainQuantity(c.id,(c.collectionQuantity||1)+1)}>+</button></div></div>)}</div>:<p className="collection-empty">Nenhuma carta do Deck Principal corresponde aos filtros.</p>}</section>
-    <section className={`extra-collection deck-build-zone deck-extra-zone ${deckDrag?"is-dragging":""}`} onDragOver={event=>event.preventDefault()} onDrop={event=>{event.preventDefault();handleDeckDrop("extra")}}><header><b>Deck Extra</b><span>{filteredSelectedExtra.length} de {selectedExtra.length} Imagens · arraste Imagens para cá.</span></header>{filteredSelectedExtra.length?<div className="card-library deck-selected-library">{filteredSelectedExtra.map(c=><div className="deck-card-entry" key={c.id} draggable onDragStart={()=>setDeckDrag({cardId:c.id,zone:"extra"})} onDragEnd={()=>setDeckDrag(null)}><OriginalCard card={c} small onClick={()=>setShowInspector(c)}/><button className="deck-remove-card" aria-label={`Remover ${c.name} do Deck Extra`} onClick={()=>toggleExtraCard(c.id,false)}>Remover</button></div>)}</div>:<p className="collection-empty">{selectedExtra.length?"Nenhuma Imagem corresponde aos filtros.":"Este herói não possui Imagens selecionadas no Deck Extra."}</p>}</section>
-    <section className={`deck-library-zone ${deckDrag?"is-drop-target":""}`} onDragOver={event=>event.preventDefault()} onDrop={event=>{event.preventDefault();handleDeckDrop("collection")}}><header><b>Coleção disponível</b><span>{filteredAvailableDeckPool.length} cartas legais para {selectedDeck.name} · solte aqui para remover uma cópia.</span></header>{filteredAvailableDeckPool.length?<div className="card-library deck-available-library">{filteredAvailableDeckPool.map(c=>{const zone=c.imageCard?"extra":"main",quantity=selectedMainQuantity(c.id),inExtra=activeUserDeck.extra.includes(c.id),disabled=zone==="main"?(quantity>=MAX_COPIES||mainDeckCopies>=MAIN_DECK_SIZE):inExtra;return <div className="deck-card-entry available" key={c.id} draggable onDragStart={()=>setDeckDrag({cardId:c.id,zone:"collection"})} onDragEnd={()=>setDeckDrag(null)}><OriginalCard card={c} small onClick={()=>setShowInspector(c)}/><button className="deck-add-card" disabled={disabled} onClick={()=>addDeckCard(c.id,zone)}>{zone==="main"?quantity?`+ cópia (${quantity}/${MAX_COPIES})`:"+ adicionar":"+ Deck Extra"}</button></div>})}</div>:<p className="collection-empty">Nenhuma carta legal corresponde aos filtros.</p>}</section>
+    <div className="collection-toolbar" role="search"><label className="collection-search-field"><span>Buscar cartas</span><input type="search" value={collectionQuery} onChange={event=>setCollectionQuery(event.target.value)} placeholder="Nome, efeito, palavra-chave…"/></label><label><span>Tipo</span><select value={collectionType} onChange={event=>setCollectionType(event.target.value as "Todas"|CardType)}><option>Todas</option>{(["Criatura","Feitiço","Artefato","Encanto","Terreno"] as CardType[]).map(type=><option key={type}>{type}</option>)}</select></label><label><span>Mostrar</span><select value={collectionMembership} onChange={event=>setCollectionMembership(event.target.value as "Todas"|"No deck"|"Fora do deck")}><option>Todas</option><option>No deck</option><option>Fora do deck</option></select></label><label><span>Ordenar</span><select value={collectionSort} onChange={event=>setCollectionSort(event.target.value as "Custo"|"Nome"|"Tipo")}><option>Custo</option><option>Nome</option><option>Tipo</option></select></label><button type="button" className="collection-filter-reset" disabled={!collectionQuery&&collectionType==="Todas"&&collectionMembership==="Todas"&&collectionSort==="Custo"} onClick={()=>{setCollectionQuery("");setCollectionType("Todas");setCollectionMembership("Todas");setCollectionSort("Custo")}}>Limpar filtros</button><output aria-live="polite">{filteredAvailableDeckPool.length} carta(s)</output></div>
+    <section className={`deck-build-zone deck-main-zone ${deckDrag?"is-dragging":""}`} onDragOver={event=>event.preventDefault()} onDrop={event=>{event.preventDefault();handleDeckDrop("main")}}><header><b>Deck Principal</b><span>{mainDeckCopies}/{MAIN_DECK_SIZE} cartas · gerencie por qualquer botão − / + ou arraste.</span></header>{filteredSelectedPool.length?<div className="card-library deck-selected-library">{filteredSelectedPool.map(c=>{const quantity=c.collectionQuantity||1;return <article className="deck-card-entry is-in-deck" key={c.id} draggable onDragStart={()=>setDeckDrag({cardId:c.id,zone:"main"})} onDragEnd={()=>setDeckDrag(null)}><OriginalCard card={c} small onClick={()=>setShowInspector(c)}/><strong className="deck-entry-name" title={c.name}>{c.name}</strong><DeckQuantityControls cardName={c.name} quantity={quantity} canIncrease={quantity<MAX_COPIES&&mainDeckCopies<MAIN_DECK_SIZE} onDecrease={()=>setMainQuantity(c.id,quantity-1)} onIncrease={()=>setMainQuantity(c.id,quantity+1)}/></article>})}</div>:<p className="collection-empty">Nenhuma carta do Deck Principal corresponde aos filtros.</p>}</section>
+    <section className={`extra-collection deck-build-zone deck-extra-zone ${deckDrag?"is-dragging":""}`} onDragOver={event=>event.preventDefault()} onDrop={event=>{event.preventDefault();handleDeckDrop("extra")}}><header><b>Deck Extra</b><span>{filteredSelectedExtra.length} de {selectedExtra.length} Imagens · inclusão individual, sem limite de cópias.</span></header>{filteredSelectedExtra.length?<div className="card-library deck-selected-library">{filteredSelectedExtra.map(c=><article className="deck-card-entry is-in-deck" key={c.id} draggable onDragStart={()=>setDeckDrag({cardId:c.id,zone:"extra"})} onDragEnd={()=>setDeckDrag(null)}><OriginalCard card={c} small onClick={()=>setShowInspector(c)}/><strong className="deck-entry-name" title={c.name}>{c.name}</strong><button type="button" className="deck-extra-toggle is-selected" aria-pressed="true" aria-label={`Remover ${c.name} do Deck Extra`} onClick={()=>toggleExtraCard(c.id,false)}>✓ No Deck Extra</button></article>)}</div>:<p className="collection-empty">{selectedExtra.length?"Nenhuma Imagem corresponde aos filtros.":"Este herói não possui Imagens selecionadas no Deck Extra."}</p>}</section>
+    <section className={`deck-library-zone ${deckDrag?"is-drop-target":""}`} onDragOver={event=>event.preventDefault()} onDrop={event=>{event.preventDefault();handleDeckDrop("collection")}}><header><b>Coleção disponível</b><span>{filteredAvailableDeckPool.length} cartas legais para {selectedDeck.name} · adicione e remova diretamente aqui.</span></header>{filteredAvailableDeckPool.length?<div className="card-library deck-available-library">{filteredAvailableDeckPool.map(c=>{const zone=c.imageCard?"extra":"main",quantity=selectedMainQuantity(c.id),inExtra=activeUserDeck.extra.includes(c.id);return <article className={`deck-card-entry available ${quantity||inExtra?"is-in-deck":""}`} key={c.id} draggable onDragStart={()=>setDeckDrag({cardId:c.id,zone:"collection"})} onDragEnd={()=>setDeckDrag(null)}><OriginalCard card={quantity?{...c,collectionQuantity:quantity}:c} small onClick={()=>setShowInspector(c)}/><strong className="deck-entry-name" title={c.name}>{c.name}</strong>{zone==="main"?<DeckQuantityControls cardName={c.name} quantity={quantity} canIncrease={quantity<MAX_COPIES&&mainDeckCopies<MAIN_DECK_SIZE} onDecrease={()=>setMainQuantity(c.id,quantity-1)} onIncrease={()=>setMainQuantity(c.id,quantity+1)}/>:<button type="button" className={`deck-extra-toggle ${inExtra?"is-selected":""}`} aria-pressed={inExtra} onClick={()=>toggleExtraCard(c.id,!inExtra)}>{inExtra?"✓ No Deck Extra":"+ Adicionar ao Extra"}</button>}</article>})}</div>:<p className="collection-empty">Nenhuma carta legal corresponde aos filtros.</p>}</section>
    </div></div>
   </section>}
     {screen==="setup"&&<section className="match-setup"><button className="back" onClick={()=>setScreen("menu")}>← Menu</button><div className="setup-head"><p>{mode==="online"?"SALA MULTIPLAYER":"PREPARE O TESTE"}</p><h2>{mode==="online"?"Crie ou entre em uma sala":"Selecione os dois decks"}</h2><span>{mode==="online"?"Compartilhe o link da sala; os dois jogadores escolhem seus decks antes de iniciar.":"As listas usam até três cópias e sempre totalizam 49 cartas."}</span></div>
--- a/app/user-deck.mjs
+++ b/app/user-deck.mjs
@@ -86,7 +86,7 @@
   return validated.deck;
 }
 
-export function validateUserDeck(input, catalog) {
+function validateUserDeckInput(input, catalog, { allowIncomplete = false } = {}) {
   const errors = [];
   const record = input && typeof input === "object" && !Array.isArray(input) ? input : null;
   if (!record) return { ok: false, errors: ["deck payload must be an object"], deck: null, mainCount: 0 };
@@ -123,7 +123,7 @@
     mainCount += quantity;
   }
   if (rawMain.length > 128) errors.push("main deck has too many unique entries");
-  if (mainCount !== MAIN_DECK_SIZE) errors.push("main deck must contain exactly " + MAIN_DECK_SIZE + " cards");
+  if (!allowIncomplete && mainCount !== MAIN_DECK_SIZE) errors.push("main deck must contain exactly " + MAIN_DECK_SIZE + " cards");
 
   const rawExtra = Array.isArray(record.extra) ? record.extra : [];
   if (!Array.isArray(record.extra)) errors.push("extra deck must be an array");
@@ -155,6 +155,18 @@
   };
 }
 
+export function validateUserDeck(input, catalog) {
+  return validateUserDeckInput(input, catalog);
+}
+
+/* Deck building is an incremental flow. A safe draft may contain fewer than
+   49 cards while the player edits it, but it must still obey every identity,
+   availability, copy-limit and Extra Deck rule. Match entry points continue
+   to use validateUserDeck(), which requires the authoritative 49-card total. */
+export function validateUserDeckDraft(input, catalog) {
+  return validateUserDeckInput(input, catalog, { allowIncomplete: true });
+}
+
 export function expandUserDeckMain(userDeck, catalog, idFactory = (cardId, copy) => cardId + "-" + copy) {
   const validated = validateUserDeck(userDeck, catalog);
   if (!validated.ok || !validated.deck) throw new Error("invalid user deck: " + validated.errors.join("; "));
--- a/app/user-deck.d.mts
+++ b/app/user-deck.d.mts
@@ -19,5 +19,6 @@
 export function cardAllowedInDeckZone(heroId: string, card: DeckCatalogCard, zone: "main" | "extra"): boolean;
 export function defaultUserDeck(heroId: DeckId, catalog: DeckCatalogCard[], name?: string): UserDeck;
 export function validateUserDeck(input: unknown, catalog: DeckCatalogCard[]): UserDeckValidation;
+export function validateUserDeckDraft(input: unknown, catalog: DeckCatalogCard[]): UserDeckValidation;
 export function expandUserDeckMain<T extends DeckCatalogCard>(userDeck: UserDeck, catalog: T[], idFactory?: (cardId: string, copy: number) => string): T[];
 export function resolveUserDeckExtra<T extends DeckCatalogCard>(userDeck: UserDeck, catalog: T[]): T[];
--- a/app/ui-overrides.css
+++ b/app/ui-overrides.css
@@ -1242,3 +1242,100 @@
    P1 PERSISTENT DECK BUILDER
    ========================================================================== */
 .screen-decks .deck-name-field{display:grid;gap:.32rem;margin:.7rem 0 .35rem}.screen-decks .deck-name-field span{font-size:.68rem;letter-spacing:.08em;text-transform:uppercase;opacity:.72}.screen-decks .deck-name-field input{width:100%;box-sizing:border-box;border:1px solid rgba(255,255,255,.14);background:rgba(5,8,16,.7);color:inherit;border-radius:.55rem;padding:.58rem .65rem;font:inherit}.screen-decks .deck-edit-actions{display:flex;gap:.45rem;flex-wrap:wrap;margin:.55rem 0}.screen-decks .deck-edit-actions button{flex:1 1 7rem;min-height:2.15rem}.screen-decks .deck-build-zone,.screen-decks .deck-library-zone{border:1px solid rgba(255,255,255,.1);border-radius:.9rem;padding:.7rem;transition:border-color .16s ease,background .16s ease}.screen-decks .deck-build-zone.is-dragging,.screen-decks .deck-library-zone.is-drop-target{border-color:color-mix(in srgb,var(--deck,#d9b864) 68%,white);background:color-mix(in srgb,var(--deck,#d9b864) 8%,transparent)}.screen-decks .deck-selected-library,.screen-decks .deck-available-library{align-items:start}.screen-decks .deck-card-entry{position:relative;display:grid;gap:.32rem;min-width:0}.screen-decks .deck-card-entry[draggable=true]{cursor:grab}.screen-decks .deck-card-entry[draggable=true]:active{cursor:grabbing}.screen-decks .deck-quantity-controls{display:grid;grid-template-columns:1fr auto 1fr;align-items:center;gap:.3rem}.screen-decks .deck-quantity-controls button,.screen-decks .deck-add-card,.screen-decks .deck-remove-card{min-height:2rem;border-radius:.45rem;font-weight:700}.screen-decks .deck-quantity-controls b{text-align:center;min-width:1.4rem}.screen-decks .deck-add-card,.screen-decks .deck-remove-card{width:100%;font-size:.72rem;letter-spacing:.02em}.screen-decks .deck-library-zone{grid-column:1/-1}.screen-decks .deck-library-zone>header,.screen-decks .deck-build-zone>header{display:flex;justify-content:space-between;gap:1rem;align-items:baseline;flex-wrap:wrap}.screen-decks .deck-library-zone>header span,.screen-decks .deck-build-zone>header span{opacity:.72;font-size:.76rem}.screen-decks .deck-validity.is-invalid{line-height:1.35}.screen-decks .deck-card-entry:focus-within{outline:2px solid color-mix(in srgb,var(--deck,#d9b864) 72%,white);outline-offset:3px}@media(max-width:900px){.screen-decks .deck-library-zone>header,.screen-decks .deck-build-zone>header{align-items:flex-start;flex-direction:column;gap:.25rem}}
+
+/* ==========================================================================
+   P1 DECK BUILDER MANAGEMENT
+   ========================================================================== */
+.screen-decks .collection-lists > .collection-toolbar{
+  grid-template-columns:minmax(12rem,2fr) repeat(3,minmax(6.7rem,1fr));
+}
+.screen-decks .collection-filter-reset{
+  align-self:end;min-height:2.4rem;padding:.5rem .72rem;border:1px solid #7fdde34a;
+  border-radius:.4rem;background:#10252dcc;color:#dff8fa;
+  font:800 clamp(.62rem,.72vw,.72rem)/1 system-ui;letter-spacing:.04em;text-transform:uppercase;
+}
+.screen-decks .collection-toolbar .collection-filter-reset{grid-column:1/2;justify-self:start}
+.screen-decks .collection-toolbar>output{grid-column:2/-1;justify-self:end}
+.screen-decks .collection-filter-reset:disabled{opacity:.35}
+.screen-decks .deck-capacity{
+  position:relative;min-height:1.45rem;margin:.4rem 0;overflow:hidden;
+  border:1px solid #ffffff1f;border-radius:999px;background:#03080dcc;
+}
+.screen-decks .deck-capacity>span{
+  position:absolute;inset:0 auto 0 0;width:var(--deck-progress,0%);
+  background:linear-gradient(90deg,color-mix(in srgb,var(--deck,#d9b864) 68%,#14252d),color-mix(in srgb,var(--deck,#d9b864) 88%,white));
+  transition:width .22s ease;
+}
+.screen-decks .deck-capacity>b{
+  position:relative;z-index:1;display:grid;min-height:1.45rem;place-items:center;
+  color:#fff;font:900 .68rem/1 system-ui;text-shadow:0 1px 3px #000;
+}
+.screen-decks .collection-lists .card-library.deck-selected-library,
+.screen-decks .collection-lists .card-library.deck-selected-library:has(> :nth-child(21)){
+  grid-template-columns:repeat(auto-fill,minmax(clamp(6rem,7.4vw,7.2rem),1fr))!important;
+  gap:clamp(.55rem,.9vw,.85rem)!important;
+}
+.screen-decks .collection-lists .card-library.deck-available-library,
+.screen-decks .collection-lists .card-library.deck-available-library:has(> :nth-child(21)){
+  grid-template-columns:repeat(auto-fill,minmax(clamp(6.5rem,8vw,7.8rem),1fr))!important;
+  gap:clamp(.65rem,1vw,.95rem)!important;max-height:clamp(24rem,58vh,42rem)!important;
+}
+.screen-decks .deck-card-entry{
+  align-content:start;padding:.48rem;border:1px solid #ffffff12;border-radius:.72rem;
+  background:linear-gradient(180deg,#0c1d24e8,#061116e8);
+  transition:border-color .16s ease,background .16s ease,box-shadow .16s ease;
+}
+.screen-decks .deck-card-entry.is-in-deck{
+  border-color:color-mix(in srgb,var(--deck,#d9b864) 55%,#ffffff22);
+  background:linear-gradient(180deg,color-mix(in srgb,var(--deck,#d9b864) 12%,#0c1d24),#061116);
+  box-shadow:inset 0 0 0 1px color-mix(in srgb,var(--deck,#d9b864) 12%,transparent);
+}
+.screen-decks .deck-card-entry>.card-frame{
+  display:block!important;width:min(100%,7.8rem)!important;max-width:7.8rem!important;
+  min-width:0!important;aspect-ratio:5/7!important;justify-self:center!important;
+}
+.screen-decks .deck-entry-name{
+  min-width:0;overflow:hidden;color:#edf7f8;
+  font:800 clamp(.62rem,.76vw,.73rem)/1.2 system-ui;text-align:center;
+  text-overflow:ellipsis;white-space:nowrap;
+}
+.screen-decks .deck-quantity-controls{grid-template-columns:2.05rem minmax(2.35rem,1fr) 2.05rem;gap:.28rem}
+.screen-decks .deck-quantity-controls button{
+  display:grid;min-width:0;min-height:2.05rem;place-items:center;padding:0;
+  border:1px solid #8adce45c;border-radius:.48rem;background:#14313a;
+  color:#f5fbfb;font:900 1.18rem/1 system-ui;
+}
+.screen-decks .deck-quantity-controls button:disabled{opacity:.28}
+.screen-decks .deck-quantity-controls output{
+  display:flex;min-width:0;align-items:baseline;justify-content:center;
+  color:#f0cf7a;font-family:system-ui;
+}
+.screen-decks .deck-quantity-controls output b{font-size:1rem}
+.screen-decks .deck-quantity-controls output small{color:#8fa6ad;font-size:.62rem;font-weight:800}
+.screen-decks .deck-extra-toggle{
+  width:100%;min-height:2.05rem;padding:.38rem .42rem;border:1px solid #8adce45c;
+  border-radius:.48rem;background:#14313a;color:#e8f8fa;
+  font:800 clamp(.58rem,.68vw,.68rem)/1.15 system-ui;
+}
+.screen-decks .deck-extra-toggle.is-selected{border-color:#68dda77d;background:#173d2dcc;color:#a7f0ca}
+@media(max-width:72rem){
+  .screen-decks .collection-lists > .collection-toolbar{grid-template-columns:1fr 1fr}
+  .screen-decks .collection-search-field{grid-column:1/-1}
+  .screen-decks .collection-toolbar .collection-filter-reset{grid-column:1/2}
+  .screen-decks .collection-toolbar>output{grid-column:2/3}
+}
+@media(max-width:48rem){
+  .screen-decks .collection-lists > .collection-toolbar{grid-template-columns:1fr 1fr}
+  .screen-decks .collection-search-field{grid-column:1/-1}
+  .screen-decks .collection-lists .card-library.deck-selected-library,
+  .screen-decks .collection-lists .card-library.deck-selected-library:has(> :nth-child(21)),
+  .screen-decks .collection-lists .card-library.deck-available-library,
+  .screen-decks .collection-lists .card-library.deck-available-library:has(> :nth-child(21)){
+    grid-template-columns:repeat(auto-fill,minmax(clamp(6.1rem,29vw,7.2rem),1fr))!important;
+  }
+}
+@media(max-width:56.25rem){
+  .screen-decks .deck-detail{grid-template-columns:minmax(0,1fr)!important}
+  .screen-decks .deck-detail>.hero-guide,
+  .screen-decks .deck-detail>.collection-lists{grid-column:1!important;width:100%!important}
+}
--- a/app/match-ui.css
+++ b/app/match-ui.css
@@ -564,3 +564,25 @@
   .extra-deck-dialog,.search-deck-dialog { width: 92vw !important; max-width: 92vw !important; height: 84dvh !important; max-height: 84dvh !important; }
   .extra-card-grid,.search-card-grid { grid-template-columns: repeat(auto-fill,minmax(clamp(3.1rem,14vw,4.35rem),1fr)) !important; }
 }
+
+/* Every enabled button communicates clickability consistently. Existing
+   component-specific transforms and shadows remain authoritative; this layer
+   adds a shared light/ring response and a clear keyboard focus treatment. */
+button:disabled{cursor:not-allowed}
+button:focus-visible{outline:2px solid #f3ca69;outline-offset:2px}
+@media (hover:hover) and (pointer:fine){
+  button:where(:not(:disabled)){
+    cursor:pointer;
+    transition-property:filter,outline-color,background-color,border-color,box-shadow;
+    transition-duration:.16s;
+    transition-timing-function:ease;
+  }
+  button:where(:not(:disabled)):hover{
+    filter:brightness(1.13) saturate(1.08);
+    outline:1px solid #f0ca6b99;
+    outline-offset:2px;
+  }
+  button.original-card:where(:not(:disabled)):hover{filter:brightness(1.08) saturate(1.06)}
+  button:where(:not(:disabled)):active{filter:brightness(.96) saturate(1.04)}
+}
+@media (prefers-reduced-motion:reduce){button{transition-duration:.01ms!important}}
--- a/tests/user-deck-model.test.mjs
+++ b/tests/user-deck-model.test.mjs
@@ -8,6 +8,7 @@
   deckIds,
   defaultUserDeck,
   validateUserDeck,
+  validateUserDeckDraft,
 } from "../app/user-deck.mjs";
 
 const catalog = JSON.parse(await readFile(new URL("../app/cards.generated.json", import.meta.url), "utf8"));
@@ -65,3 +66,18 @@
   duplicate.main.push({ ...duplicate.main[0] });
   assert.equal(validateUserDeck(duplicate, catalog).ok, false);
 });
+
+test("safe incomplete drafts persist without weakening match validation", () => {
+  const deck = defaultUserDeck("saymon", catalog, "Saymon em edição");
+  const draft = structuredClone(deck);
+  draft.main[0].quantity -= 1;
+
+  const draftValidation = validateUserDeckDraft(draft, catalog);
+  assert.equal(draftValidation.ok, true, draftValidation.errors.join("; "));
+  assert.equal(draftValidation.mainCount, MAIN_DECK_SIZE - 1);
+  assert.deepEqual(draftValidation.deck, draft);
+  assert.equal(validateUserDeck(draft, catalog).ok, false);
+
+  draft.main[0].quantity = MAX_COPIES + 1;
+  assert.equal(validateUserDeckDraft(draft, catalog).ok, false);
+});
--- a/tests/persistent-deck-builder.test.mjs
+++ b/tests/persistent-deck-builder.test.mjs
@@ -4,10 +4,13 @@
 
 const page=await readFile(new URL("../app/page.tsx",import.meta.url),"utf8");
 const model=await readFile(new URL("../app/user-deck.mjs",import.meta.url),"utf8");
+const matchCss=await readFile(new URL("../app/match-ui.css",import.meta.url),"utf8");
 
 test("deck builder hydrates and persists only validated deck snapshots",()=>{
  assert.match(page,/USER_DECK_STORAGE_KEY/);
  assert.match(page,/localStorage\.getItem\(USER_DECK_STORAGE_KEY\)/);
+ assert.match(page,/validateUserDeckDraft\(candidates\[deck\.id\],cards\)/);
+ assert.match(page,/validateUserDeckDraft\(userDecks\[deck\.id\],cards\)/);
  assert.match(page,/validation\.ok&&validation\.deck/);
  assert.match(page,/localStorage\.setItem\(USER_DECK_STORAGE_KEY/);
 });
@@ -22,6 +25,24 @@
  assert.match(page,/undoActiveDeck/);
 });
 
+test("collection cards expose direct reversible controls and useful filters",()=>{
+ assert.match(page,/function DeckQuantityControls/);
+ assert.match(page,/quantity=\{quantity\}/);
+ assert.match(page,/collectionMembership/);
+ assert.match(page,/collectionSort/);
+ assert.match(page,/Limpar filtros/);
+ assert.match(page,/clearActiveDeck/);
+ assert.match(page,/aria-pressed=\{inExtra\}/);
+ assert.match(page,/Rascunho salvo/);
+});
+
+test("all enabled buttons expose hover, active and keyboard focus feedback",()=>{
+ assert.match(matchCss,/button:where\(:not\(:disabled\)\):hover/);
+ assert.match(matchCss,/button:where\(:not\(:disabled\)\):active/);
+ assert.match(matchCss,/button:focus-visible/);
+ assert.match(matchCss,/button:disabled\{cursor:not-allowed\}/);
+});
+
 test("online and bot matches consume the validated UserDeck",()=>{
  assert.match(page,/roomAction\("select",\{heroId,userDeck:validation\.deck/);
  assert.match(page,/start\(mine,enemy,0,30,mineValidation\.deck/);
--- a/tests/card-preview-collection-quality.test.mjs
+++ b/tests/card-preview-collection-quality.test.mjs
@@ -65,11 +65,15 @@
 test("collection search, type filtering and deck validation are visible and responsive", () => {
   assert.match(page, /useDeferredValue\(collectionQuery\)/);
   assert.match(page, /collectionType==="Todas"\|\|card\.type===collectionType/);
+  assert.match(page, /collectionMembership/);
+  assert.match(page, /compareCollectionCards/);
   assert.match(page, /validateUserDeck\(activeUserDeck,cards\)/);
   assert.match(page, /mainDeckCopies=deckValidation\.mainCount/);
   assert.match(page, /MAIN_DECK_SIZE/);
   assert.match(page, /className={`deck-validity/);
   assert.match(page, /disabled=\{!deckListValid\}/);
   assert.match(collectionCss, /\.collection-toolbar/);
+  assert.match(collectionCss, /\.deck-quantity-controls/);
+  assert.match(collectionCss, /\.deck-card-entry\.is-in-deck/);
   assert.match(collectionCss, /\.deck-validity\.is-invalid/);
 });
--- a/tests/graveyard-collection-priority-ui.test.mjs
+++ b/tests/graveyard-collection-priority-ui.test.mjs
@@ -16,7 +16,7 @@
     readFile(new URL("../app/ui-overrides.css", import.meta.url), "utf8"),
   ]);
   assert.match(page, /collectionQuantity:entry\.quantity/);
-  assert.match(page, /activeUserDeck\.main\.map/);
+  assert.match(page, /activeUserDeck\.main\.flatMap/);
   assert.match(page, /collection-copy-count/);
   assert.match(css, /screen-decks \.collection-lists[^}]*overflow: hidden !important/s);
   assert.match(css, /card-library:has\(> :nth-child\(21\)\)/);
